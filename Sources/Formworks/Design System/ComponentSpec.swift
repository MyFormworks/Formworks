//
//  ComponentSpec.swift
//  
//
//  Created by Artur Carneiro on 02/10/20.
//

import UIKit


/// Used to specificate the components settings.
enum ComponentSpec {
    
    /// Used to spacificate the `SingleLine` component.
    enum SingleLine {
        // Title container specs
        static let titleWidthMultipler: CGFloat = 0.8
        static let requiredWidthMultiplier: CGFloat = 0.2
        static let requiredHeightMultiplier: CGFloat = 0.8
    }
}
